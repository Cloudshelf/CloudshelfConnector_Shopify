query ProductsTest($first: Int!) {
    products(first: $first) {
        edges {
            node {
                id
            }
        }
        pageInfo {
            hasNextPage
            hasPreviousPage
            endCursor
            startCursor
        }
    }
}
